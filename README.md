# SourceBridge

Base scaffold for a Source SDK 2013 main menu called **SourceBridge** with:

- Start Hosting
- Options
- Quit

## Quick start

```bash
./scripts/bootstrap_sdk2013_sourcebridge.sh
```

Then follow `sdk2013_overlay/README.md` to hook the panel into your SDK 2013 client mode startup.
